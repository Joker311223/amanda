Page({
  data: {},
  onLoad() {
    // 页面加载
  },

  onShow() {
    // const db = wx.cloud.database();

    // 查询数据（示例：从表`users`中查找年龄>18的用户）
    // db.collection("assigments")
    //   .where({ age: db.command.gt(18) })
    //   .get()
    //   .then((res) => {
    //     console.log('cyagsc',res.data);
    //   });

    // // 插入数据
    // db.collection("assigments").add({
    //   data: { name: "张三", age: 20 },
    // });
    // 页面显示
  },
});
