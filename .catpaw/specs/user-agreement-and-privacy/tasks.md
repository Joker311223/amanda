# 实施计划 - 用户协议与隐私政策

## 1. 概述
本计划将分步实现注册页面的协议勾选功能及协议详情展示页面。

## 2. 任务列表

- [x] 1. 注册新页面 `pages/terms/terms`
  - 在 `app.json` 中添加页面路径。
  - _需求: 3.1_

- [x] 2. 实现协议详情页面 `pages/terms/terms`
  - [x] 2.1 编写 `terms.wxml` 结构
    - 使用 `rich-text` 或 `text` 展示协议内容。
    - _需求: 3.1, 3.2_
  - [x] 2.2 编写 `terms.js` 逻辑
    - 根据 `onLoad` 传入的 `type` 参数加载对应的协议内容。
    - 补充《用户服务协议》和《隐私政策》的具体文本内容。
    - _需求: 3.1, 4.1, 4.2_
  - [x] 2.3 编写 `terms.wxss` 样式
    - 设置标题、正文、段落间距等样式。
    - _需求: 3.1_

- [x] 3. 修改注册页面 `pages/onboarding/onboarding`
  - [x] 3.1 修改 `onboarding.wxml`
    - 在提交按钮上方增加 `checkbox` 勾选框。
    - 增加协议链接文本，绑定点击事件。
    - _需求: 1.1, 1.2_
  - [x] 3.2 修改 `onboarding.js`
    - 在 `data` 中增加 `isAgreed` 状态。
    - 实现 `onAgreementChange` 处理勾选状态变化。
    - 实现 `goToAgreement` 处理协议跳转。
    - 修改 `submitUserInfo` 增加 `isAgreed` 校验逻辑。
    - _需求: 1.3, 2.1_
  - [x] 3.3 修改 `onboarding.wxss`
    - 调整勾选框和协议文本的布局与样式。
    - _需求: 1.1_

- [x] 4. 检查点 - 确保所有功能正常运行
  - 验证勾选框状态切换。
  - 验证未勾选时的提交拦截。
  - 验证协议页面的跳转及内容显示。

- [~]* 5. 编写测试用例
  - [~]* 5.1 编写单元测试
    - 验证协议内容的完整性。
    - _需求: 4.1, 4.2_
  - [~]* 5.2 编写属性测试
    - **属性 1: 协议跳转一致性**
    - **属性 2: 提交前置校验**
    - **属性 3: 协议内容加载正确性**
    - _验证: 需求 1.3, 2.1, 3.1_

## 3. 备注
- 任务中带有 `*` 的为可选任务，可根据需要选择是否执行。
- 协议内容由 AI 根据合规要求补充。

